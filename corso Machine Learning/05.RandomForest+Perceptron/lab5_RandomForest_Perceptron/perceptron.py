import numpy as np


class Perceptron(object):

    def __init__(self):
        #instantiate you perceptron: what do you need?

    def predict(self, inputs):
        #return the prediction, given an input

    def train_p_rule(self, training_inputs, labels):  #PERCEPTRON RULE
        #update perceptron weights with the Perceptron rule: update weights at each training step 
        
    def train_d_rule(self, training_inputs, labels):  #DELTA RULE
        #update perceptron weights with the Delta rule: update weights only at the end of the epoch
